# 🚀 QUICKSTART: Nongor Premium Bot v3.0

**Get your bot running in 3 minutes!**

---

## 1️⃣ Essential Prerequisite
**Python 3.14 Users Only:**
You must install the pre-release libraries to avoid crashes.
```bash
pip install --upgrade --pre -r requirements.txt
```

---

## 2️⃣ Fast Setup
Run the setup wizard to configure your bot automatically:

```bash
python setup.py
```
*Follow the prompts to enter your Bot Token and Admin ID.*

---

## 3️⃣ Run It!
Launch the bot:

```bash
python bot_v2.py
```

---

## 4️⃣ Usage Cheat Sheet

| Action | Command |
|--------|---------|
| **Start / Menu** | `/start` |
| **AI Chat** | Click `🤖 AI Assistant` button |
| **Exit AI** | Type `/menu` |
| **Check Site** | Click `🌐 Website` button |

---

## 🆘 Common Issues

**"Module Not Found"**
> Run `pip install -r requirements.txt` again.

**"Access Denied"**
> Add your Telegram User ID to `.env` file (variable: `ADMIN_USER_IDS`).

**"AI Not responding"**
> Get a free API key from [Google AI Studio](https://aistudio.google.com/app/apikey) and add it to `.env`.

---

**Need more help?** Read the full `README.md`.
